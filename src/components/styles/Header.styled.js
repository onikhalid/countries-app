import styled from 'styled-components';

export const StyledHeader = styled.header`

    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-block: 1.5rem;
    padding-inline: min(5vw, 3.5rem);
    box-shadow: 0px 2px 15px rgba(0, 0, 0, 0.25);
    height: 5vh;
    margin-bottom: 2vh;


    h3{
        cursor: pointer;
        font-weight: 400;
    }
`
